# FastAPI — Task List

1. **Hello World & Routing** — App creation, path operations, path/query params
2. **Request & Response Models** — Pydantic models, validation, serialization
3. **Dependency Injection** — Depends(), scoped dependencies, overrides for testing
4. **Async & Middleware** — async def vs def, middleware, CORS, error handlers
5. **Database Integration** — SQLAlchemy/SQLModel, sessions, migrations with Alembic
6. **Auth** — OAuth2, JWT tokens, securing endpoints
7. **Background Tasks & WebSockets** — BackgroundTasks, WebSocket endpoints
8. **Testing** — TestClient, pytest fixtures, mocking dependencies
9. **Capstone: REST API** — Build a complete CRUD API with auth, DB, validation, tests, and OpenAPI docs
