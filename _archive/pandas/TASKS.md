# Pandas — Task List

1. **Series & DataFrames** — Creating, inspecting, dtypes, index
2. **Selection & Filtering** — loc, iloc, boolean indexing, query()
3. **Data Cleaning** — Missing values, duplicates, type conversion, string methods
4. **GroupBy & Aggregation** — groupby, agg, transform, pivot tables
5. **Merging & Joining** — merge, join, concat, handling keys
6. **Time Series** — DateTime index, resampling, rolling windows
7. **I/O** — read_csv, read_json, read_parquet, to_* methods
8. **Capstone: Data Pipeline** — Load a messy real-world dataset, clean it, analyze it, output summary stats and a clean parquet file
