# scikit-learn — Task List

1. **Estimator API** — fit/predict/transform pattern, datasets, train/test split
2. **Preprocessing** — Scaling, encoding, imputation, ColumnTransformer
3. **Linear Models** — Linear regression, logistic regression, regularization
4. **Tree-Based Models** — Decision trees, random forests, gradient boosting
5. **Model Evaluation** — Metrics, cross-validation, confusion matrix, ROC/AUC
6. **Pipelines** — Pipeline, FeatureUnion, parameter grids
7. **Hyperparameter Tuning** — GridSearchCV, RandomizedSearchCV
8. **Unsupervised Learning** — KMeans, PCA, dimensionality reduction
9. **Capstone: ML Pipeline** — End-to-end classification on a real dataset: preprocess, train, evaluate, select best model, serialize with joblib
