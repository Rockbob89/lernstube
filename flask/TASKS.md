# Flask — Task List

1. **App & Routing** — Flask app, routes, methods, request/response objects
2. **Templates & Static Files** — Jinja2, template inheritance, serving static assets
3. **Blueprints & Structure** — Application factory, blueprints, config
4. **Database** — Flask-SQLAlchemy, migrations with Flask-Migrate
5. **Capstone: Web App** — Build a small web app (different use case than the FastAPI capstone) — focus on where Flask fits vs FastAPI
