# PyTorch — Task List

1. **Tensors** — Creating tensors, operations, GPU placement, NumPy interop
2. **Autograd & Backprop** — Computational graph, .backward(), gradients
3. **nn.Module** — Building models, layers, forward(), parameters
4. **Training Loop** — Loss functions, optimizers, batching, DataLoader
5. **CNNs** — Conv2d, pooling, building an image classifier
6. **Transfer Learning** — Pretrained models, fine-tuning, freezing layers
7. **Saving & Serving** — state_dict, TorchScript, ONNX export
8. **Capstone: Image Classifier** — Train a CNN on a real dataset, evaluate, export for serving
