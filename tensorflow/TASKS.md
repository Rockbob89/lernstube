# TensorFlow — Task List

1. **Tensors & Eager Execution** — tf.Tensor, operations, tf.function, GradientTape
2. **Keras Sequential & Functional API** — Building models, layers, compile/fit/evaluate
3. **Data Pipeline** — tf.data.Dataset, map, batch, prefetch, TFRecord
4. **CNNs & Transfer Learning** — Conv layers, pretrained models, fine-tuning
5. **Saving & Serving** — SavedModel, TFLite, TF Serving
6. **Capstone: Model Pipeline** — End-to-end: data pipeline → train → evaluate → export as SavedModel
