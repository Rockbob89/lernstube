# Pulumi — Task List

1. **Pulumi vs Terraform** — State management, language support, when to choose which
2. **Project Setup** — pulumi new, stacks, config, secrets
3. **Resources & Providers** — Defining resources, provider configuration, resource options
4. **Inputs, Outputs & Apply** — Pulumi's async model, Output[T], apply(), interpolation
5. **Component Resources** — Reusable abstractions, ComponentResource class
6. **Stack References & Config** — Multi-stack architectures, cross-stack references
7. **Testing** — Unit tests, property tests, integration tests
8. **Capstone: Multi-Cloud Deploy** — Deploy a small app across two clouds (or cloud + K8s) using Pulumi with Python
